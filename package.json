{
  "name": "diagnostico-backend",
  "version": "1.0.0",
  "description": "Backend para geracao de PDF integrado à Vercel",
  "main": "api/generate-pdf.js",
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pdfkit": "^0.13.0",
    "resend": "^3.5.0"
  }
}
